name: CI

on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

env:
  RUSTUP_TOOLCHAIN: 1.98.1

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      RUSTFLAGS: -C target-cpu=native
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain and formatter
        run: rustup toolchain install 1.98.1 --profile minimal --component rustfmt
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Check Archive V2 wire boundary
        run: bash scripts/check-archive-v2-wire-boundary.sh
      - name: Test workspace target migration checks
        run: python3 scripts/test_check_workspace_targets.py
      - name: Check workspace
        run: cargo check --workspace --all-targets --locked
      - name: Check opt-in Blockzilla contributor and repair binaries
        run: cargo check --locked -p blockzilla --features developer-tools,archive-repair-tools --all-targets
      - name: Check user-program index commands and operational tools
        run: cargo check --locked -p blockzilla-user-program-index --features developer-tools --all-targets
      - name: Test workspace
        run: cargo test --workspace --all-targets --locked
      - name: Test user-program index operational tools
        run: cargo test --locked -p blockzilla-user-program-index --features developer-tools --all-targets
      - name: Test Archive V2 metadata repair
        run: cargo test --locked -p blockzilla --features archive-repair-tools --bin archive-v2-normalize-metadata-trusted
      - name: Test Hivezilla shred reconstruction feature
        run: |
          cargo test --locked -p hivezilla --features shred-reconstruction --lib ingest::shred_compact::tests
          cargo test --locked -p hivezilla --features shred-reconstruction --bin shred-fec-gap-trial
          cargo test --locked -p hivezilla --features shred-reconstruction --bin shred-epoch-audit
      - name: Build and read the Archive V2 fixture
        run: |
          output="$RUNNER_TEMP/blockzilla-fixture"
          cargo run --locked -p blockzilla -- \
            build-archive-v2-hot-blocks \
            crates/old-faithful/of-car-reader/benches/fixtures/epoch-157-biggest.car \
            "$output" \
            --max-blocks 1 \
            --no-access
          test -s "$output/archive-v2-blocks.zstd"
          test -s "$output/archive-v2-blocks.index"
          test -s "$output/archive-v2-meta.wincode"
          cargo run --locked -p blockzilla -- \
            bench-archive-v2-hot-blocks \
            "$output/archive-v2-blocks.zstd" \
            --workers 1 \
            --chunk-size 1

  worker:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Worker build tools
        run: |
          rustup toolchain install 1.98.1 --profile minimal --target wasm32-unknown-unknown
          cargo install worker-build --locked --version 0.8.5
      - name: Build Archive V2 Edgezilla Worker
        working-directory: edgezilla/get-block
        run: worker-build --release . --no-default-features --features worker
      - name: Test Archive V2 Edgezilla Worker
        env:
          RUSTFLAGS: -C target-cpu=native
        run: cargo test --locked -p blockzilla-get-block --no-default-features --features worker --lib
      - name: Build Old Faithful Edgezilla Worker
        working-directory: edgezilla/of-get-block
        run: worker-build --release .

  hivezilla-ops:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install Rust toolchain
        run: rustup toolchain install 1.98.1 --profile minimal
      - name: Check portable launch scripts
        run: |
          /bin/sh -n \
            hivezilla/service/scripts/generate-grpc-replication-pki.sh \
            hivezilla/service/scripts/generate-grpc-pull-pki.sh \
            hivezilla/service/scripts/run-grpc-raw-receiver.sh \
            hivezilla/service/scripts/run-grpc-raw-replicator.sh \
            hivezilla/service/scripts/run-grpc-raw-pull-source.sh \
            hivezilla/service/scripts/run-grpc-raw-pull-client.sh \
            hivezilla/service/scripts/run-grpc-receiver-bridge.sh
      - name: Test Python operational helpers
        run: python3 hivezilla/service/scripts/test_ingest_status_server.py
      - name: Build native Hivezilla recorder support binary
        env:
          RUSTFLAGS: -C target-cpu=native
        run: cargo build --locked -p hivezilla --bin hivezilla
      - name: Test shell operational helpers
        run: |
          bash hivezilla/service/scripts/test-linux-raw-grpc-cache-supervisor.sh
          bash hivezilla/service/scripts/test-linux-raw-grpc-recorder-alerts.sh
          bash hivezilla/service/scripts/test-run-grpc-raw-wrappers.sh
          bash hivezilla/service/scripts/test-generate-grpc-replication-pki.sh

  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Check local Markdown links
        uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
        with:
          args: --offline --root-dir "${{ github.workspace }}" --no-progress './**/*.md'
          fail: true

  secrets:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - name: Scan reachable history with Gitleaks
        env:
          GITLEAKS_VERSION: 8.30.1
          GITLEAKS_SHA256: 551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb
        run: |
          archive="gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          curl --fail --location --silent --show-error \
            --output "$RUNNER_TEMP/$archive" \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/$archive"
          echo "$GITLEAKS_SHA256  $RUNNER_TEMP/$archive" | sha256sum --check --strict
          tar -xzf "$RUNNER_TEMP/$archive" -C "$RUNNER_TEMP" gitleaks
          "$RUNNER_TEMP/gitleaks" git . \
            --config=.gitleaks.toml \
            --log-opts=--all \
            --redact=100 \
            --no-banner \
            --no-color
